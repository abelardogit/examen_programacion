/*
NO EDITES ESTA CLASE
 */
package reto1;

public class Validator {
    public static boolean isNIEValid(String aNIE) {
        // 0. Formato del NIE (no tienes que validarlo): "A9999999B" (1 letra + 7 dígitos + 1 letra)
        // 1. 'A' solo puede ser 'X', 'Y' o 'Z' (no tienes que validarlo).
        // 2. Sustituye su valor. Usa el método NIEDB.getValueFirstLetter para ello.
        // 3. Convierte la cadena numérica a un número. ❌ No uses JAVA en este paso.
        // 4. Halla el resto de este número con 23.
        // 5. Usa el método NIEDB.getLetter para saber si la letra del NIE proporcionado es correcta.


        return true; // Devolved el valor que consideréis
    }
}
